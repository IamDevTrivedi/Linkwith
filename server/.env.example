# MongoDB Configuration
# Provide the MongoDB connection string (e.g., mongodb://localhost:27017/linkwith or a MongoDB Atlas URI)
MONGODB_URL=

# Server Configuration
# Specify the port for the backend server (e.g., 5000)
PORT=5000

# CORS Configuration
# Define allowed origins for CORS (e.g., http://localhost:3000 for development)
CORS_ORIGIN=

# JWT Configuration
# Secret key for signing JSON Web Tokens (generate a strong, random string)
JWT_SECRET=

# Node Environment
# Set the environment (use 'development' for local, 'production' for deployed app)
NODE_ENV=development
# NODE_ENV=production

# Email Configuration
# Configure email service (e.g., Brevo, Gmail, etc.)
# For Brevo, get SMTP details from: https://www.brevo.com/
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
SENDER_EMAIL=