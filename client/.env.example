# Backend API Configuration
# Specify the URL of the backend server (e.g., http://localhost:5000 for development)
REACT_APP_BACKEND_URL=your_backend_url

# Node Environment
# Set the environment (use 'development' for local, 'production' for deployed app)
NODE_ENV=development
# NODE_ENV=production